const ConfettiExplosion = () => (
  <div className="absolute inset-0 pointer-events-none">
    {/* Confetti canvas or SVG shapes would go here */}
    🎉🎉🎉
  </div>
);

export default ConfettiExplosion;
